# Market data provider configuration
# ----------------------------------
# This app uses SYNTHETIC MOCK DATA only — no live broker credentials are
# kept in the deployed Worker. The default below is the only value used
# in production; setting anything else is silently ignored and falls back
# to mock. The variable is still read so the diagnostic endpoint can show
# whether the dashboard was deployed with the expected configuration.
MARKET_PROVIDER=mock
